[@abox-dev/sdk](../../README.md) / [sandboxApi](../README.md) / SandboxIamTokenType

# Type Alias: SandboxIamTokenType

> **SandboxIamTokenType** = `"JWT-SVID"` \| `string` & `object`

Workload token type. `'JWT-SVID'` is the only type the API accepts in this
version; the set is defined server-side and may grow, so any string is
allowed.
